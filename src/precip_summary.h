
#pragma once

#include "nbm_data.h"

/**
 * Print a summary of the probability of reaching certain precipitation amounts.
 *
 * \param nbm the data to summarize.
 */
void show_precip_summary(struct NBMData const *nbm);
