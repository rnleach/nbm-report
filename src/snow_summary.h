#pragma once

#include "nbm_data.h"

/**
 * Print a summary of the probability of reaching certain snow amounts.
 *
 * \param nbm the data to summarize.
 */
void show_snow_summary(struct NBMData const *nbm);
